import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: 86,
    height: 86,
  },
  imageContainer: {
    padding: 2.5,
    borderRadius: 50,
  },
});

export default styles;
